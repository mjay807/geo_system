# 核心依赖
streamlit>=1.30,<2
pandas>=2.0,<3
plotly>=5.0,<6

# LangChain 核心
langchain-core>=1.2,<2
langchain-community>=0.4,<1

# LangChain 提供商（按需安装）
langchain-openai>=1.1,<2
langchain-groq>=1.1,<2
langchain-deepseek>=1.0,<2
langchain-moonshot>=0.1.0,<1

# 通义千问（ChatTongyi 依赖 dashscope，见 langchain-community）
dashscope>=1.0,<2

# 平台同步功能依赖
httpx>=0.24.0
pyperclip>=1.8.2

# ============================================================
# 可选依赖（根据需要取消注释）
# ============================================================

# 文心一言（百度）
# qianfan>=0.1.0

# 豆包（字节跳动）
# pip install 'volcengine-python-sdk[ark]'

# LangChain 文心一言支持
# langchain-wenxin>=0.1.0,<1
